# KiranaIQ — Live Demo (temporary tunnels)

> These Cloudflare quick tunnels run only while your machine is on and the processes are active. For permanent hosting, use Vercel + Render (see `DEPLOYMENT.md`).

| Service | URL |
|---------|-----|
| **Frontend** | https://engineering-lake-hobby-spent.trycloudflare.com |
| **Backend API** | https://message-require-refresh-combo.trycloudflare.com |

**Try it:** Open the frontend → Sign up → **Forecast** → Pre-fill → Generate forecast.

**Local (recommended for dev):**
- API: http://127.0.0.1:8000
- UI: `cd frontend && npm run dev` → http://localhost:5173
